PORT TO XBOX by HCF
(still not finished, it has bugs, but it works for the main part of scenarios)

This library need the "freetype" library 
(you should put the include path in Project -> Properties -> C/C++ -> Additional include directories)
